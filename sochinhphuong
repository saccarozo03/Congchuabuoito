#include<stdio.h>
#include<math.h>
int sochinhphuong(long long a){
    int n=sqrt(a);
    if(sqrt(a)==n){
        return 1;
    }else{
    return 0;}
}
int main(){
    long long n;
    scanf("%lld",&n);
    if(sochinhphuong(n)){
        printf("YES");
    }else{
        printf("NO");
    }
    }
